# Base PostgreSQL image
FROM postgres:15

# Set environment variables
ENV POSTGRES_DB=fitness_booking
ENV POSTGRES_USER=fitness_user
ENV POSTGRES_PASSWORD=your_secure_password

# Expose PostgreSQL port
EXPOSE 5432

